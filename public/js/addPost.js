const newPostBtn = document.querySelector('#add-post-btn');
const createPostCard = document.querySelector('#create-post-card');

const newFormHandler = (event) => {
    event.preventDefault();

    const postTitle = document.querySelector('')
}